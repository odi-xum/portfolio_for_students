# ── База данных ───────────────────────────────────────────────────────
# PostgreSQL (через Docker Compose)
DATABASE_URL=postgresql://diplom:diplom_pass@127.0.0.1:5432/diplom
# SQLite (для локальной разработки без Docker)
# DATABASE_URL=sqlite:///database.db

# ── Flask ─────────────────────────────────────────────────────────────
SECRET_KEY=generate-a-real-secret-key-here
FLASK_DEBUG=1
